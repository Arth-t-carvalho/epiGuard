/**
 * navigation.js
 * Responsável por inicializar ícones e comportamentos comuns de navegação
 */

document.addEventListener('DOMContentLoaded', () => {
    // Inicializar ícones do Lucide
    if (window.lucide) {
        window.lucide.createIcons();
    }
});
